.page-header
  %h2= t('.title')

- content_for :right_column do
  = render 'comfy/admin/cms/sites/mirrors', :object => @variant

= comfy_form_for @variant, :as => :variant, :url => {:action => :update} do |form|
  = render form
